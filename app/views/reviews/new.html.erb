<h2> Add a new review </h2>

  <p>
    <%= simple_form_for [ @cruise, @review ] do |f| %>
    <%= f.input :content %>
    <%= f.input :rating %>
    <%= f.submit "Submit review", class: "btn btn-primary" %>
  <% end %>
  </p>
